{
  "name": "aqua-lumina",
  "version": "1.0.0",
  "private": true,
  "description": "AQUA LUMINA — 次世代型海洋施設（デモサイト）",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node scripts/server.js",
    "start:lan": "node scripts/server.js --listen",
    "share": "node scripts/share-url.js"
  },
  "dependencies": {
    "serve-handler": "^6.1.7"
  },
  "devDependencies": {
    "localtunnel": "^2.0.2",
    "serve": "^14.2.4"
  }
}
